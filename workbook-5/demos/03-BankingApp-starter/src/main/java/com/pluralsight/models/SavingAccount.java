package com.pluralsight.models;

public class SavingAccount extends BankAccount
{
    private static final double MIN_BALANCE = 25;

    public SavingAccount(int accountNumber, String owner, double balance) { super(accountNumber, owner, balance)}

    @Override
    public boolean canWithdraw(do)
}
